# نسخة التسليم النهائية — محلل جودة الاختبارات

هذه النسخة مهيأة لتجربة العرض التجريبي Controlled Pilot، مع تثبيت السكوب وعدم إضافة خدمات مدفوعة أو ترحيلات قاعدة بيانات جديدة.

## أهم التعديلات المدمجة

- استعادة تقسيم جداول الصح والخطأ إلى سؤال رئيسي وبنود فرعية.
- دعم الترقيم الهرمي مثل `Q1.1` و`Q3(a)` مع عدم احتساب السؤال الحاوي مرتين.
- تنظيف درجات السؤال من النص، مع منع تفسير الأرقام التقنية كدرجات.
- قراءة مجموع الاختبار ومقارنته بمجموع درجات الأسئلة.
- اعتماد درجة السؤال الأب مرة واحدة عند وجودها، وجمع درجات الأبناء فقط عندما تكون درجة الأب غير موجودة؛ غياب درجات الأبناء لا يعطل التحقق إذا كانت درجة الأب واضحة.
- فصل مرجع PLO ومجال المخرج عن نص CLO.
- فصل الساعات المتوقعة عن نص موضوع المقرر.
- تقليل المواد المساندة إلى السياق المرتبط صراحة بالسؤال فقط.
- ربط مخطط/كود السؤال الرئيسي بالأجزاء الفرعية مع إبقائه كسياق واحد.
- جعل التقييم الدلالي المحلي اقتراحياً واستبعاد الأحكام المحلية غير الموثوقة من مقام الدرجة.
- تسمية الدرجة في الوضع المحلي بأنها درجة أولية محلية.
- تجميع التحذيرات المتكررة وإخفاء التشخيصات التقنية داخل قسم مطوي.
- تبسيط بداية تجهيز الأسئلة إلى استخراج PDF أو لصق/استيراد قائمة، مع إبقاء الإضافة اليدوية داخل شاشة المراجعة.

## التشغيل السريع على Windows

يمكن تشغيل الملف التالي من PowerShell:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\RUN_FINAL_PILOT.ps1
```

أو التشغيل يدوياً:

```powershell
docker compose -p exam_quality_fixed up -d postgres chromadb
docker compose -p exam_quality_fixed run --rm --build backend alembic upgrade head
docker compose -p exam_quality_fixed up --build -d
```

ثم افتح:

```text
http://localhost:5173
```

لا تستخدم `docker compose down -v` لأنه يحذف بيانات قاعدة البيانات.

## حدود النسخة التجريبية

- Gemini معطل افتراضياً، ولا يلزم لاستخراج الأسئلة أو حساب الدرجة المحلية.
- العلاقات المحلية بين الأسئلة وCLOs والموضوعات اقتراحات للمراجعة وليست خرائط رسمية.
- الكتابة اليدوية، المسح شديد الرداءة، والجداول المعقدة جداً تحتاج مراجعة بشرية.
- لا يبدأ التحليل إلا بعد تأكيد مراجعة الأسئلة.

## التحقق المنجز

- يحتوي المشروع على 827 اختبار Backend. تم تشغيلها على مجموعات صغيرة بسبب مهلة بيئة التنفيذ؛ لم يظهر أي فشل، ووصلت المجموعة الأخيرة إلى 100% قبل أن تتجاوز عملية الإغلاق مهلة الأداة.
- نجح اختبار قبول فعلي لملف ITDB211: 23 سجلاً، منها 19 سؤالاً قابلاً للتقييم و4 حاويات، و5 بنود صح/خطأ، وسياق مساند واحد مرتبط بـQ4.
- نجح فحص صياغة ملفات TypeScript/TSX المعدلة.
- لم تُشغّل اختبارات Frontend الكاملة بسبب تعذر جلب حزمة من مستودع npm في بيئة الاختبار، لذلك يلزم اختبار الواجهة يدوياً قبل العرض.
- تم تحديث اختبارات الدلالة القديمة لتتوافق مع سياسة النسخة الحالية: عدم استخدام غياب التشابه اللفظي المحلي كدليل قطعي على عدم التغطية.

## قبل العرض

نفّذ تحليلاً جديداً، وتحقق من التسجيل، رفع الملفات، مراجعة الأسئلة، التأكيد، النتائج، تنزيل التقرير، الرجوع، وحذف تحليل تجريبي. استخدم بيانات مقرر مطابقة للملفات المعروضة.
