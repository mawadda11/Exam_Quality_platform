# إصلاح ترقيم الأسئلة الهرمي والجداول — 2026-08-05

## ما تم إصلاحه

- الاحتفاظ بالأرقام المركبة كما هي: `Q1.1` و`Q1.2` بدل اختصارها إلى `Q1`.
- الاحتفاظ بالأجزاء الحرفية: `Q3(a)` و`Q3(b)` و`Q3(c)` وربطها تلقائيًا بالسؤال الأب `Q3`.
- تحويل صفوف جدول True/False إلى أسئلة فرعية مرتبطة بالسؤال الأب، حتى عندما يبدأ الجدول في الصفحة التالية.
- عدم توزيع درجة السؤال الأب تلقائيًا على صفوف جدول True/False؛ تظل درجات الأبناء غير محددة ما لم تكن مكتوبة صراحة.
- عدم اعتبار خطوط الإجابة المنقطة أو المسطرة فراغات Fill-in-the-blank.
- استبعاد تذييلات الصفحات التجريبية وأرقام الصفحات من نص السؤال.
- منع خطوط الإجابة من خفض جودة النص الرقمي ودفع الصفحة إلى OCR بلا حاجة.
- تحسين تمييز أسئلة MCQ عندما تحتوي على مصطلحات تقنية مثل SQL أو command.
- تحسين تمييز أسئلة SQL/code بصفتها `code_question`.
- دعم عنوان عمود `Assessment` الشائع في جدول TP-153، واستخراج سجلات التقييم منه.
- تحديث رسالة المراجعة لتشير إلى صفحة PDF المعروضة يسارًا بدل صورة قصاصة محذوفة.

## التحقق على الحالة الأولى من حزمة الاختبارات

- تم استخراج 23 سجل سؤال منظمًا.
- `Q1` أب و `Q1.1` إلى `Q1.8` أبناء، ولكل ابن 4 خيارات.
- `Q2` أب و `Q2.1` إلى `Q2.5` أبناء True/False من الجدول عبر الصفحتين.
- `Q3` أب و `Q3(a)` إلى `Q3(c)` أبناء Short Answer بلا فراغات وهمية.
- `Q4` أب و `Q4(a)` إلى `Q4(c)` أبناء SQL، والدرجات 3 لكل جزء.
- تم استخراج 5 سجلات Assessment من توصيف المقرر بدل صفر.

## الاختبارات

- نجحت 88 اختبارات مستهدفة تغطي التصنيف، PDF الرقمي، البنية، الجداول، وTP-153.
- نجح استيراد تطبيق FastAPI.
- محاولة تشغيل مجموعة Backend الكاملة تجاوزت مهلة بيئة التنفيذ قبل اكتمالها؛ لم يظهر فشل قبل انتهاء المهلة.
- Ruff غير متوفر في بيئة التنفيذ الحالية، لذلك لم يُشغّل هنا.

## ملفات الكود المعدلة

- `backend/app/services/extraction/line_classification.py`
- `backend/app/services/extraction/digital_pdf_extractor.py`
- `backend/app/services/extraction/exam_structure.py`
- `backend/app/services/extraction/text_quality.py`
- `backend/app/services/extraction/digital_tp153_extractor.py`
- `backend/app/services/extraction/review_workflow.py`
- اختبارات Regression ذات الصلة داخل `backend/tests/`

لا يوجد Migration جديد، ولم يُشغّل Gemini، ولم تُضف خدمة مدفوعة.
